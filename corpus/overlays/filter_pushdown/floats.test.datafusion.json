{
  "schema_version": 1,
  "records": [
    {
      "record": 3,
      "original_sql": "SELECT n, x FROM example.echo((SELECT * FROM (VALUES (1, 1.5::DOUBLE), (2, 2.5::DOUBLE), (3, NULL::DOUBLE), (4, 3.5::DOUBLE)) v(n, x)))\nWHERE x = 2.5::DOUBLE ORDER BY n;",
      "kind": "out_of_scope",
      "reason": "DataFusion 55 cannot pass n and x as two top-level scalar-subquery columns to the table function. Struct packing would change the VGI input schema and filtered floating-point column."
    },
    {
      "record": 5,
      "original_sql": "SELECT n, x FROM example.echo((SELECT * FROM (VALUES (1, 0.0::DOUBLE), (2, 1.5::DOUBLE), (3, 2.5::DOUBLE)) v(n, x)))\nWHERE x > 1.0::DOUBLE ORDER BY n;",
      "kind": "out_of_scope",
      "reason": "DataFusion 55 cannot pass n and x as two top-level scalar-subquery columns to the table function. Struct packing would change the VGI input schema and range-filtered floating-point column."
    },
    {
      "record": 7,
      "original_sql": "SELECT n, x FROM example.echo((SELECT * FROM (VALUES (1, 1.5::FLOAT), (2, 2.5::FLOAT), (3, 3.5::FLOAT)) v(n, x)))\nWHERE x = 2.5::FLOAT ORDER BY n;",
      "kind": "out_of_scope",
      "reason": "DataFusion 55 cannot pass n and x as two top-level scalar-subquery columns to the table function. Struct packing would change the VGI input schema and filtered Float32 column."
    },
    {
      "record": 9,
      "original_sql": "SELECT n, x FROM example.echo((SELECT * FROM (VALUES (1, 'nan'::DOUBLE), (2, 1.0::DOUBLE), (3, 'nan'::DOUBLE)) v(n, x)))\nWHERE x = 'nan'::DOUBLE ORDER BY n;",
      "kind": "out_of_scope",
      "reason": "DataFusion 55 cannot pass n and x as two top-level scalar-subquery columns to the table function. Struct packing would change the VGI input schema and would not validate NaN filter pushdown."
    },
    {
      "record": 11,
      "original_sql": "SELECT n, x FROM example.echo((SELECT * FROM (VALUES (1, '-inf'::DOUBLE), (2, 0.0::DOUBLE), (3, 'inf'::DOUBLE)) v(n, x)))\nWHERE x > 1.0::DOUBLE ORDER BY n;",
      "kind": "out_of_scope",
      "reason": "DataFusion 55 cannot pass n and x as two top-level scalar-subquery columns to the table function. Struct packing would change the VGI input schema and would not validate infinity range pushdown."
    }
  ]
}
