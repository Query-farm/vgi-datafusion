{
  "schema_version": 1,
  "records": [
    {
      "record": 39,
      "original_sql": "SELECT s, t FROM example.echo((\n  SELECT s, t FROM (VALUES ('a', 1::BIGINT),\n                           ('b', NULL::BIGINT),\n                           ('c', 3::BIGINT)) v(s, t)\n)) WHERE s >= 'b' AND t IS NOT NULL ORDER BY s;",
      "kind": "out_of_scope",
      "reason": "This compound predicate requires both s and t, but DataFusion 55 permits only one top-level scalar-subquery column for the table-function argument. Struct packing would change the VGI input schema and filter paths."
    }
  ]
}
