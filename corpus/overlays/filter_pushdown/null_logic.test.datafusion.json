{
  "schema_version": 1,
  "records": [
    {
      "record": 9,
      "original_sql": "SELECT n, t FROM example.echo((\n  SELECT n, t FROM (VALUES (1::BIGINT, 1::BIGINT),\n                           (1::BIGINT, NULL::BIGINT),\n                           (5::BIGINT, 1::BIGINT),\n                           (5::BIGINT, NULL::BIGINT),\n                           (NULL::BIGINT, 1::BIGINT),\n                           (NULL::BIGINT, NULL::BIGINT))\n                    v(n, t)\n)) WHERE n = 1 AND t = 1 ORDER BY n;",
      "kind": "out_of_scope",
      "reason": "The SQL three-valued AND assertion needs both n and t, but DataFusion 55 permits only one top-level scalar-subquery column for the table function. Struct packing would change the VGI filter paths."
    },
    {
      "record": 11,
      "original_sql": "SELECT n, t FROM example.echo((\n  SELECT n, t FROM (VALUES (1::BIGINT, 1::BIGINT),\n                           (1::BIGINT, NULL::BIGINT),\n                           (5::BIGINT, 1::BIGINT),\n                           (5::BIGINT, NULL::BIGINT),\n                           (NULL::BIGINT, 1::BIGINT),\n                           (NULL::BIGINT, 5::BIGINT),\n                           (NULL::BIGINT, NULL::BIGINT))\n                    v(n, t)\n)) WHERE n = 1 OR t = 1 ORDER BY n NULLS LAST, t NULLS LAST;",
      "kind": "out_of_scope",
      "reason": "The SQL three-valued OR assertion needs both n and t, but DataFusion 55 permits only one top-level scalar-subquery column for the table function. Struct packing would change the VGI filter paths."
    },
    {
      "record": 19,
      "original_sql": "SELECT n, t FROM example.echo((\n  SELECT n, t FROM (VALUES (1::BIGINT, 'a'),\n                           (1::BIGINT, NULL),\n                           (2::BIGINT, 'b'),\n                           (2::BIGINT, NULL)) v(n, t)\n)) WHERE n = 1 AND t IS NULL ORDER BY n, t;",
      "kind": "out_of_scope",
      "reason": "The equality-plus-validity assertion needs both n and t, but DataFusion 55 permits only one top-level scalar-subquery column for the table function. Struct packing would change the VGI filter paths."
    }
  ]
}
