{
  "schema_version": 1,
  "records": [
    {
      "record": 3,
      "original_sql": "SELECT n, b FROM example.echo((SELECT * FROM (VALUES\n  (1, '\\x00\\x01\\x02'::BLOB),\n  (2, '\\xff\\xee\\xdd'::BLOB),\n  (3, NULL::BLOB)\n) v(n, b)))\nWHERE b = '\\xff\\xee\\xdd'::BLOB ORDER BY n;",
      "kind": "out_of_scope",
      "reason": "The runner can map DuckDB BLOB literals to DataFusion Binary, but DataFusion 55 cannot pass both n and b as top-level columns through this scalar-subquery table-function argument. Struct packing would change the VGI input schema."
    },
    {
      "record": 5,
      "original_sql": "SELECT n, b FROM example.echo((SELECT * FROM (VALUES (1, 'data'::BLOB), (2, NULL::BLOB)) v(n, b)))\nWHERE b IS NULL ORDER BY n;",
      "kind": "out_of_scope",
      "reason": "The runner can map DuckDB BLOB literals to DataFusion Binary, but DataFusion 55 cannot pass both n and b as top-level columns through this scalar-subquery table-function argument. Struct packing would change the VGI input schema."
    }
  ]
}
