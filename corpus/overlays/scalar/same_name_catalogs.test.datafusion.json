{
  "schema_version": 1,
  "records": [
    {
      "record": 9,
      "original_sql": "USE a.main;",
      "kind": "out_of_scope",
      "reason": "DataFusion 55 does not implement the DuckDB USE statement or a mutable SQL catalog search path; qualified VGI catalog dispatch is covered by the preceding records."
    },
    {
      "record": 10,
      "original_sql": "SELECT test_same_name_catalog(42);",
      "replacement_sql": "SELECT a.main.test_same_name_catalog(42);",
      "kind": "equivalent_sql",
      "reason": "DataFusion does not implement DuckDB's mutable USE search path; the fully qualified call selects the same catalog implementation through DataFusion's existing catalog API."
    },
    {
      "record": 11,
      "original_sql": "USE b.main;",
      "kind": "out_of_scope",
      "reason": "DataFusion 55 does not implement the DuckDB USE statement or a mutable SQL catalog search path; qualified VGI catalog dispatch is covered by the preceding records."
    },
    {
      "record": 12,
      "original_sql": "SELECT test_same_name_catalog(42);",
      "replacement_sql": "SELECT b.main.test_same_name_catalog(42);",
      "kind": "equivalent_sql",
      "reason": "DataFusion does not implement DuckDB's mutable USE search path; the fully qualified call selects the same catalog implementation through DataFusion's existing catalog API."
    },
    {
      "record": 13,
      "original_sql": "USE memory.main;",
      "kind": "out_of_scope",
      "reason": "DataFusion 55 does not implement the DuckDB USE statement or a mutable SQL catalog search path."
    }
  ]
}
