{
  "schema_version": 1,
  "records": [
    {
      "record": 16,
      "original_sql": "SELECT COUNT(*) >= 1 FROM duckdb_logs() WHERE type = 'VGI' AND message LIKE '%result_cache.hit%';",
      "kind": "out_of_scope",
      "reason": "This assertion specifically requires a compressed disk-tier hit after memory eviction; DataFusion's VGI cache is memory-only and does not claim disk compression support."
    }
  ]
}
